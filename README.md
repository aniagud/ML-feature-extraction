# ML-feature-extraction
ML: Experimental comparison of feature extraction methods (PCA, KPCA, LDA and no extraction) for various classifiers (kNN, SVM, CART, GNB, MLP and neural networks) for medical and synthetic data (both balanced and imbalanced) in python.
